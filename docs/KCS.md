# KCS — skip (2026-09-10)

KuCoin Earn / Staking 2.0 is **CEX custody** (3d flexible). Do not wrap.

On-chain LST: sKCS.io `0x00eE2d494258D6C5A30D6B6472A09b27121Ef451` on **KCC 321**.
RPC `https://rpc-mainnet.kcc.network`: `symbol=sKCS`, `totalSupply≈3046`, native KCS on contract ≈16718, `exchangeRate()` raw `3.419e21` (not 1e18-per-share; ≈1.12 vs supply). Explorer ~213 holders. DEX vol ~0. Proxy; explorer warns bytecode ≠ verified.

LayerZero metadata: **no KCC / KuCoin chain**. HyperEVM code at that address: empty.

Official KCC validator stake is native (min 1 KCS), no transferable receipt we would wrap.

NotThisBatch. Revisit only if a live EVM LST with real TVL appears **and** LZ lists KCC.
